# {{PROJECT}} 검증 총괄 (EXPERIMENTS) — 전 트랙 블록 아키텍처·현황·산출물 매핑

> ① 이 문서는 **지도·현황판**이다 — 수치 정본은 `{{수치 정본 registry 경로}}` (인용은 ★확정 행에서만)
> ② 공통 규칙: [{{공통 규칙 문서}}]({{경로}}) — **registry 등록은 사용자 검토 필수**
> ③ **갱신 책임: 트랙 상태를 바꾼 세션이 이 문서의 현황판·간트를 갱신한다**

---

## 1. 전 테스트 트랙 블록 아키텍처

<!-- 공유 인프라 → 트랙 분기 ASCII 도식. 예:
        {{공유 인프라 스택}}
        ┌─────┴─────┬───────────┐
   {{트랙1}}    {{트랙2}}    {{트랙3}}
-->

**원칙**: 모든 트랙이 같은 실물 스택을 공유한다 — 트랙 간 차이는 ①검증 계층 ②토글 ③결합 여부뿐.

**러너 계보**: `{{unit_run.sh}}` (단위 러너) ← `{{batch_run.sh}}` / `{{pair_run.sh}}` (배치·페어·매트릭스 러너가 단위 러너를 감싼다) <!-- 러너 스크립트 체계가 없는 프로젝트는 이 항목 삭제 -->

## 2. 트랙별 명세 (트랙당 카드 1개)

### {{트랙 이름}} — {{한 줄 설명}}

| 항목 | 내용 |
|---|---|
| 목적 | {{무엇을 검증하나}} |
| 설계·판정 정본 | `{{specs 문서 경로}}` |
| 런북 | `{{runbook 경로}}` |
| 러너/하니스 | `{{스크립트 경로}}` |
| 산출물 연결 | {{어느 기여/장·절에 들어가나}} |

## 2.5 트랙 전량 매트릭스 — 한눈 조망

> 중복을 감수하고 한 표에 모은다. 수치 정본은 registry.

| 트랙 | 설명(무엇을 검증) | 구성(규모) | 핵심 메트릭 | 기여 매핑 | 산출물 위치 | 목표(완료 조건) | 상태/블로커 |
|---|---|---|---|---|---|---|---|
| {{T1}} | {{...}} | {{n회 × 조건}} | {{지표}} | {{C1}} | {{경로}} | {{게이트}} | {{✅/🟡/⬜ + 블로커}} |

## 3. 현황 간트 ({{기준일}} 기준 · 예정 구간은 잠정 — {{조건}} 확보일에 확정)

```mermaid
gantt
    dateFormat  YYYY-MM-DD
    section {{트랙군}}
    {{완료 항목}}      :done, t1, {{시작일}}, {{종료일}}
    {{크리티컬 패스}}   :crit, t2, {{시작일}}, {{기간}}
```

## 4. 기여↔근거 매핑

### 4.1 기여(Contribution) ↔ 실험 근거

> **hedge 규율**: 미실측 주장은 가설형("~개선하는지 검정")만, 단정형("개선한다") 금지 — unhedged 주장은 측정 결과에만.

| 기여 | 주장 (hedge 규율 준수) | 실험 근거 | 상태 |
|---|---|---|---|
| {{C1}} | {{...}} | {{트랙·지표}} | {{측정 완료/가설}} |

### 4.2 방법론 강점 (방어 정본 = `{{설계 리뷰 문서}}`)

- **paired 규율**: {{조건 비교는 동일 환경 페어로 — 공통 노이즈 상쇄}}
- **ground-truth 정직성**: {{판정 기준을 실측 가능한 신호에 앵커}}
- **정직성 캡션 내장**: 한계를 결과와 같은 자리에 표기
- **재현 패키지**: {{다중 캡처}} + manifest + **레지스트리 등급제(★확정/○후보/△이슈)** + 전 런 원본 보존

### 4.3 실행 세션 시작 게이트 (실행 착수 직후 — 순서대로)

<!-- 현장 이동형 프로젝트라면 "{{환경}} 도착 직후"로 구체화. 예: 회사망/현장 도착 직후. 상시 접근 환경(웹 서비스 등)은 "실행 세션 시작 시"로 그대로 사용. -->

1. {{환경 점검}} <!-- 예: 네트워크/IP 확인·갱신 -->
2. {{스모크 1회}} — 통과 후에만 본 실행
3. {{본 실행}}
4. registry 등록은 **사용자 검토 후**

### 4.4 축적 데이터 영속성 — {{소실 시 재현 불가한 상태}} 백업

<!-- 선택 절: 런타임에 축적되어 소실 시 재현 불가한 데이터가 있을 때만. -->

| 항목 | 저장 위치 | 생존 범위 | 백업 도구 | 복원 | 주기 |
|---|---|---|---|---|---|
| {{...}} | {{volume/경로}} | {{무엇까지 살아남나}} | `{{backup 스크립트}}` | {{절차}} | {{시점}} |

## 5. 진행판 요약 (상세·수치 = 레지스트리)

| 항목 | 상태 |
|---|---|
| {{항목}} | ✅ / ⬜ |
