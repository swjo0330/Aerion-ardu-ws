# {{PROJECT}}-{{정체성 두문자}}

> {{프로젝트 1문장 정의}} <!-- 예: 두문자라면 각 글자 해설을 바로 아래에 -->
> **궁극 업무목표를 항상 인지하고 작업한다**: {{최종 목표 1문장}}

---

## 1. 전체 아키텍처

<!-- 레이어별 컴포넌트 + 통신 프로토콜 주석이 달린 계층 도식(ASCII/mermaid).
     각 화살표에 "무슨 프로토콜/채널로" 를 명기하는 것이 핵심. -->

```
{{레이어 1: 전략/조정}}
   │ ({{프로토콜 A}})
{{레이어 2: 실행 에이전트}}
   │ ({{프로토콜 B}})
{{레이어 3: 실물/디바이스}}
```

## 2. 컴포넌트

| 컴포넌트 | 역할 | 입출력 | 상태 |
|---|---|---|---|
| {{이름}} | {{1줄}} | {{입력→출력}} | ✅ 구현 완료 / 📌 미래 구현 |

## 3. 데이터 흐름

<!-- 핵심 파이프라인을 단계 순서로: {{입력}} → {{처리1}} → {{처리2}} → {{출력}} -->

## 4. 인터페이스 스키마 (정본)

<!-- 컴포넌트 간 계약(JSON/msg 등)은 여기가 단일 정본 — 코드와 어긋나면 여기를 갱신. ICD로 성장하는 절. -->

### {{인터페이스 이름}} ({{생산자}} → {{소비자}})

```json
{ "{{field}}": "{{type — 의미}}" }
```

## N.x {{결정명}} 설계 확정 ({{YYYY-MM-DD}})

<!-- 설계 결정은 별도 문서가 아니라 이 문서 안에 날짜와 함께 누적한다.
     결정마다 이 3소절 구조 반복: -->

### 배경
{{무엇이 문제였고, 어떤 근거(측정·비교)가 있었나}} <!-- 예: 프로토콜 A vs B 호출 성능 실측 → 역할 확정 -->

### 확정 구조
{{선택한 구조 — 도식/표}}

### 역할 재정의
{{이 결정으로 바뀐 컴포넌트 역할}}

## 네이밍 컨벤션

- {{토픽/키/파일 네이밍 규칙}}

## 로드맵·구현 현황 ({{YYYY-MM-DD}} 스냅샷)

| 단계 | 내용 | 상태 |
|---|---|---|
| {{Phase N}} | {{...}} | ✅ / 🟡 / 📌 |
